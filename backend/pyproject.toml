[project]
name = "linguabar-prototype-backend"
version = "0.1.0"
description = "Prototype API for LinguaBar"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.128.0",
  "uvicorn==0.39.0",
  "pydantic==2.12.5",
]

[project.scripts]
serve = "uvicorn:main"

[tool.uv]
package = false
